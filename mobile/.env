# Backend API base URL. localhost works for the iOS Simulator (shares the
# Mac's network namespace). For a physical device or Android emulator, use
# your machine's LAN IP instead (e.g. http://192.168.1.23:3000) — localhost
# on device refers to the device itself, not your dev machine.
EXPO_PUBLIC_API_URL=http://localhost:3000
